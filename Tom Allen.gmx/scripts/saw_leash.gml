/// Leash state (stopped chasing, returning to idle spot)

if(alarm[0] <= 0)
{
    if (x > initialx)
    {
    
    }
    else if (x < initialx)
    {
    
    }
    else
    {
        state = saw_idle;
    }
}
